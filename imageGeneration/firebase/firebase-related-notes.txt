TO RUN THE FIREBASE PYTHON FUNCTION, CHANGE TO venv :
source /media2/project/imageGen/firebase/functions/venv/bin/activate

firebase login								//login to the firebase

firebase logout								//logout from the firbase

code .									//to start the vscode editor	

npm install firebase-admin@latest firebase-functions@latest		//install the latest admin and functions. Run inside the function folder
 		
firebase deploy								//to deploy the firebase function as cloud function

npm run-script lint							//check for possible errors

npm run-script build							//compile the typescript to javascript

npm run build                               //compile the typescript

firebase serve --only functions						//Emulate the firbase functions before deploying

firebase serve --only hosting						//Emulate the firbase hosting before deploying

firebase deploy  --only hosting                     //Deploy the website to firebase

firebase deploy --only functions                    //Deploy only the functions

export GOOGLE_APPLICATION_CREDENTIALS="path/to/key.json"

//To test the typescript locally using emulator
export GOOGLE_APPLICATION_CREDENTIALS="path/to/key.json"
firebase emulators:start

firebase functions:log --only <FUNCTION_NAME>                           // to get function log

 endTime: "167098744600",
 
 
 resolveJsonModule: "true"
